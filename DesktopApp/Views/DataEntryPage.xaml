<UserControl x:Class="DesktopApp.Views.DataEntryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="600"
             MinHeight="400">

    <UserControl.Resources>
        <!-- Rounded TextBox Style -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rounded Button Style -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#0A4967"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top teal line -->
        <Border Grid.Row="0"
                Height="2"
                Background="#0A4967"
                VerticalAlignment="Bottom"
                Margin="0,0,0,10"/>

        <!-- Main white panel -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="LightGray"
                BorderThickness="2"
                CornerRadius="6"
                Padding="20"
                Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Labels and Inputs -->
                <TextBlock Text="Company Name :" Grid.Row="0" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="Contact No :" Grid.Row="1" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="GSTIN :" Grid.Row="2" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="2" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="Address Line 1 :" Grid.Row="3" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="3" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="Address Line 2 :" Grid.Row="4" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="4" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="City / Pincode :" Grid.Row="5" Grid.Column="0" FontWeight="DemiBold" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox Grid.Row="5" Grid.Column="1" Height="30" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,10"/>

                <!-- Save Button -->
                <Button Grid.Row="4" Grid.Column="1"
                        Content="💾 Save"
                        HorizontalAlignment="Right"
                        Width="100"
                        Style="{StaticResource RoundedButton}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>