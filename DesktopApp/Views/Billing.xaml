<UserControl x:Class="DesktopApp.Views.Billing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:DesktopApp.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:BillingViewModel, IsDesignTimeCreatable=True}"
             Background="#F7F8FC"
             FontFamily="Segoe UI">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#495057"/>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#555" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border CornerRadius="4" Background="White" BorderBrush="#DEE2E6" BorderThickness="1">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#4D90FE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="MedicineItemTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                <TextBlock Text=" - Stock: "/>
                <TextBlock Text="{Binding Stock}"/>
                <TextBlock Text=" - ₹"/>
                <TextBlock Text="{Binding UnitPrice, StringFormat=F2}"/>
            </StackPanel>
        </DataTemplate>

        <Style x:Key="NumericUpDownStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" CornerRadius="4" Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                            </Border>
                            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0">
                                <Button Command="{Binding DataContext.IncreaseQtyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Content="▲" FontSize="6" Padding="4,0" Margin="1" Cursor="Hand"/>
                                <Button Command="{Binding DataContext.DecreaseQtyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Content="▼" FontSize="6" Padding="4,0" Margin="1" Cursor="Hand"/>
                            </StackPanel>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4D90FE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0056b3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#A0A0A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="25" BorderBrush="#DEE2E6" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Select Client" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding ClientList}" SelectedItem="{Binding SelectedClient}" Style="{StaticResource RoundedComboBoxStyle}"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="Select Medicine" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ComboBox ItemsSource="{Binding MedicineList}" SelectedItem="{Binding SelectedMedicine}" ItemTemplate="{StaticResource MedicineItemTemplate}" Style="{StaticResource RoundedComboBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,15,0">
                        <TextBlock Text="Quantity" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBox Text="{Binding NewQty, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource NumericUpDownStyle}"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="Add to Invoice" Command="{Binding AddToInvoiceCommand}" Style="{StaticResource PrimaryButtonStyle}" Background="#28A745" VerticalAlignment="Bottom"/>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Border Background="White" CornerRadius="6" Padding="25" BorderBrush="#DEE2E6" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Invoice Items" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <ListView Grid.Row="1" ItemsSource="{Binding InvoiceItems}" BorderThickness="0" HorizontalContentAlignment="Stretch">
                        <ListView.View>
                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style TargetType="GridViewColumnHeader">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Foreground" Value="#6C757D"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="0,0,0,10"/>
                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn Header="MEDICINE" Width="200" DisplayMemberBinding="{Binding ProductName}"/>
                                <GridViewColumn Header="BATCH" Width="100" DisplayMemberBinding="{Binding BatchNo}"/>
                                <GridViewColumn Header="QUANTITY" Width="80" DisplayMemberBinding="{Binding Quantity}"/>
                                <GridViewColumn Header="UNIT PRICE" Width="100" DisplayMemberBinding="{Binding UnitPrice, StringFormat=C}"/>
                                <GridViewColumn Header="TOTAL" Width="100" DisplayMemberBinding="{Binding Total, StringFormat=C}"/>
                                <GridViewColumn Header="ACTION" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Remove" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Foreground="Red" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Subtotal:" Width="100" TextAlignment="Right" Margin="0,0,10,5"/>
                    <TextBlock Text="{Binding Subtotal, StringFormat=C}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Tax (18% GST):" Width="100" TextAlignment="Right" Margin="0,0,10,5"/>
                    <TextBlock Text="{Binding Tax, StringFormat=C}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Discount:" Width="100" TextAlignment="Right" Margin="0,0,10,5"/>
                    <TextBox Text="{Binding Discount, StringFormat=F2}" Width="80" BorderBrush="#CED4DA" Padding="5,3"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="Total:" Width="100" TextAlignment="Right" Margin="0,0,10,5" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Total, StringFormat=C}" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" Content="Create Invoice" Command="{Binding CreateInvoiceCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20,0,0,0"/>
        </Grid>
    </Grid>
</UserControl>